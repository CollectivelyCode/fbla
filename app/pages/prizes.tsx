import Shell from "../lib/components/Shell";
import {RedeemablePrizes} from "../lib/components/RedeemablePrizes";

export default function Prizes() {
    return (
        <Shell>
            <RedeemablePrizes/>
        </Shell>
    )
}